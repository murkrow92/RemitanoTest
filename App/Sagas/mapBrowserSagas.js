import { takeLatest } from 'redux-saga/effects';
import { BrowserTypes } from 'Reduxes/Actions/BrowserActions';

const mapBrowserSagas = [
  // takeLatest(ProductTypes.FETCH_PRODUCT_LIST, fetchProductList)
];
export default mapBrowserSagas;
